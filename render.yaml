services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - key: BOT_TOKEN
        sync: true
    # Render автоматично надає змінну середовища RENDER_EXTERNAL_URL
    httpPort: 8443
